import styled, {ThemeProvider as BaseThemeProvider, withTheme as baseWithTheme} from "styled-components/native";

export const ThemeProvider = BaseThemeProvider;
export const withTheme = baseWithTheme;
export default styled;